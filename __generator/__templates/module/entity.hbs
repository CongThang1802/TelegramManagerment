import { AbstractEnity } from 'src/abstractions/entity.abstraction';
import { Column, Entity } from 'typeorm';

@Entity({ name: '{{name}}s' })
export class {{Case name}} extends AbstractEntity {
    
}
